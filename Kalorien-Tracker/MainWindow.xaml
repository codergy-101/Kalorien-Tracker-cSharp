<Window x:Class="Kalorien_Tracker.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Kalorien_Tracker"
        mc:Ignorable="d"
        Title="Kalorien-Tracker" Height="1050" Width="600" Background="#2E2E2E"
        Loaded="Window_Loaded" Closing="Window_Closing">
    <Window.Resources>
        <local:ProgressBarWidthConverter x:Key="ProgressBarWidthConverter"/>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#104e8b"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="BorderBrush" Value="#1a1a1a"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#083b6e"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="ProgressBar">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="10">
                            <Grid ClipToBounds="True">
                                <Rectangle Fill="{TemplateBinding Foreground}"
                                           Width="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=Value, Converter={StaticResource ProgressBarWidthConverter}}"
                                           HorizontalAlignment="Left"
                                           RadiusX="10" RadiusY="10"/>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ListBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="10">
                            <ScrollViewer Margin="0" Focusable="False">
                                <ItemsPresenter/>
                            </ScrollViewer>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="10">
                            <ScrollViewer Margin="0" x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ListBoxItem">
            <Setter Property="FontSize" Value="16"/>
        </Style>
    </Window.Resources>
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Top" Margin="10">
            <Label Content="Kalorienziel:" Foreground="White" FontSize="16" Margin="0,10,0,0" HorizontalAlignment="Center"/>
            <TextBox x:Name="CalorieGoalTextBox" Width="200" Margin="0,10,0,0" Background="#424242" Foreground="White"/>

            <Label Content="Makro-Verhältnis (P/K/F):" Foreground="White" FontSize="16" Margin="0,10,0,0" HorizontalAlignment="Center"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
                <StackPanel>
                    <Label Content="Protein" Foreground="#32CD32" FontSize="16" HorizontalAlignment="Center"/>
                    <TextBox x:Name="ProteinTextBox" Width="50" Margin="5" Background="#32CD32" Foreground="White"/>
                </StackPanel>
                <StackPanel>
                    <Label Content="KH" Foreground="#1E90FF" FontSize="16" HorizontalAlignment="Center"/>
                    <TextBox x:Name="CarbsTextBox" Width="50" Margin="5" Background="#1E90FF" Foreground="White"/>
                </StackPanel>
                <StackPanel>
                    <Label Content="Fett" Foreground="#FF4500" FontSize="16" HorizontalAlignment="Center"/>
                    <TextBox x:Name="FatTextBox" Width="50" Margin="5" Background="#FF4500" Foreground="White"/>
                </StackPanel>
            </StackPanel>

            <Button Content="Aktualisieren" Width="220" Margin="0,10,0,0" Click="UpdateSettings_Click"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
                <Button Content="&#8592;" Width="70" Click="PrevDay_Click"/>
                <Label x:Name="DateLabel" Content="{Binding CurrentDate, StringFormat=dd.MM.yyyy}" Foreground="White" FontSize="16" Margin="10,0"/>
                <Button Content="&#8594;" Width="70" Click="NextDay_Click"/>
            </StackPanel>

            <ListBox x:Name="DailySummaryListBox" Width="500" Height="300" Background="#424242" Foreground="White" Margin="0,10,0,0"/>

            <ProgressBar x:Name="CalorieProgressBar" Width="500" Height="20" Margin="0,10,0,0"/>
            <Label x:Name="ProgressLabel" Content="0 / 0 kcal (0%)" Foreground="White" FontSize="16" Margin="0,10,0,0"/>

            <TextBox x:Name="MacroTextBox" Width="500" Height="100" Background="#2E2E2E" Foreground="White" IsReadOnly="True" Margin="0,10,0,0"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
                <Button Content="Speichern" Width="120" Click="SaveData_Click"/>
                <Button Content="Laden" Width="120" Click="LoadData_Click"/>
            </StackPanel>
            <Button Content="Essen hinzufügen" Width="220" Margin="0,10,0,0" Click="OpenAddMealWindow_Click"/>
            <Button Content="Essen entfernen" Width="220" Margin="0,10,0,0" Click="RemoveFood_Click"/>
        </StackPanel>
    </Grid>
</Window>